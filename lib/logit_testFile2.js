const express = require('express');console.log({express});const app = express();console.log({app});const compression = require('compression');console.log({compression});const csurf = require('csurf');console.log({csurf});app.use(require('body-parser').json());app.use(    require('cookie-session')({        secret: 'I am nearly always happy.',        maxAge: 24 * 60 * 60 * 1000,    }));app.use(csurf());app.use(function(req, res, next) {    res.cookie('mytoken', req.csrfToken());    next();});const pwUtils = require('./pwUtils.js');console.log({pwUtils});const dbUtils = require('./dbUtils.js');console.log({dbUtils});